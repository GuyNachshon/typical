# Launch record (v0, 2026-09-20)

The launch plan, strategy notes and the Jev wire schema from the v0 launch package.
`systemone_wire_schema.md` is still current — the `/v1/systemone` wire format has not changed.

`site.png`, `site-mobile.png` and `site-live-playground.png` are screenshots of the **v0 site**,
kept as a record of what launch looked like. The current site is `site/` (see `site/README.md`);
these images do not reflect it.
