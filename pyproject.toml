[project]
name = "typical-site"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.14.0",
    "transformers>=5.17.0",
    "safetensors>=0.4.0",
    "huggingface_hub>=0.25.0",
    "numpy>=2.0.0",
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
    "datasets>=3.0.0",
]

[tool.uv]
package = false
